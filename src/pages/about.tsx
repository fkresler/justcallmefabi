import * as React from 'react';
import { Layout } from '../components/Layout';

const AboutMePage: React.FC = () => (
  <Layout>
    <h1>Here will be more info about myself further down the line!</h1>
  </Layout>
);

export default AboutMePage;
