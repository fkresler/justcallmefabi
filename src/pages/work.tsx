import * as React from 'react';
import { Layout } from '../components/Layout';

const WorkPage: React.FC = () => (
  <Layout>
    <h1>Here will be examples of my work further down the line</h1>
  </Layout>
);

export default WorkPage;
