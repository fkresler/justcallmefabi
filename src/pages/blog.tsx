import * as React from 'react';
import { Layout } from '../components/Layout';

const BlogPage: React.FC = () => (
  <Layout>Here will live all the blogging content</Layout>
);

export default BlogPage;
